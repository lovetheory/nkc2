//do sth with the post variable here

//horizontal line
if(index==0)
else
  hr
  
//post itself
.media
  .media-left.media-top
    a(href='#')
      img.media-object.avatar(src='http://bbs.kechuang.org/windid/attachment/avatar/000/00/00/10_middle.jpg' style='width:60px;height:60px;')
  .media-body.media-top
    .row
      .col-xs-12
        if(post.t&&post.t!=''&&index!=0)
          h4.d-inline #{post.t}
        p.d-inline.lighttext #{post.username} - #{dateString(post.toc)}
        case post.l
          when 'markdown'
            div!= markdown(post.c)
          when 'bbcode'
            div!= bbcode(post.c)
          when 'plain'
          default
            div!= plain(post.c)
            
