-var p = p
.panel.panel-default
  .panel-body
    p.reddish #{dateTimeString(p.tlm)} 
    p 作者(修改者)：
      -var uid = p.uidlm||p.uid
      a(href='/user_profile/#{uid}') #{uid}
    
    p 语言：#{p.l}
    
    hr.hrNarrowSpace
      
    div!=plain(p.c)
  
